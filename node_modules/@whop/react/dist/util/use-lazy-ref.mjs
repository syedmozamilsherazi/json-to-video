import { useRef } from "react";
const none = Symbol("none");
export function useLazyRef(fn) {
    const ref = useRef(none);
    if (ref.current === none) {
        ref.current = fn();
    }
    return ref;
}
