import type { StoryObj } from '@storybook/react';
import React from 'react';
import { ContextMenu } from '..';
declare const meta: {
    title: string;
    component: {
        (props: ContextMenu.ContentProps): React.JSX.Element;
        displayName: string;
    };
    args: {
        variant: "translucent";
    };
    parameters: {
        layout: string;
    };
    render: (args: ContextMenu.ContentProps) => React.JSX.Element;
    tags: string[];
};
export default meta;
type Story = StoryObj<typeof meta>;
export declare const Default: Story;
