import classNames from 'classnames';
import { Slot } from 'radix-ui';
import * as React from 'react';
import { textPropDefs } from './text.props';
const Text = (props) => {
    const { children, className, asChild = false, as: Tag = 'span', size = textPropDefs.size.default, weight = textPropDefs.weight.default, align = textPropDefs.align.default, trim = textPropDefs.trim.default, color = textPropDefs.color.default, highContrast = textPropDefs.highContrast.default, ...textProps } = props;
    return (React.createElement(Slot.Root, { "data-accent-color": color, ...textProps, className: classNames('fui-Text', className, size ? `fui-r-size-${size}` : undefined, weight ? `fui-r-weight-${weight}` : undefined, align ? `fui-r-ta-${align}` : undefined, trim ? `fui-r-lt-${trim}` : undefined, { 'fui-high-contrast': highContrast }) }, asChild ? children : React.createElement(Tag, null, children)));
};
Text.displayName = 'Text';
export { Text };
//# sourceMappingURL=text.js.map