import classNames from 'classnames';
import { Slot } from 'radix-ui';
import * as React from 'react';
import { headingPropDefs } from './heading.props';
const Heading = (props) => {
    const { children, className, asChild = false, as: Tag = 'h1', size = headingPropDefs.size.default, weight = headingPropDefs.weight.default, align = headingPropDefs.align.default, trim = headingPropDefs.trim.default, color = headingPropDefs.color.default, highContrast = headingPropDefs.highContrast.default, ...headingProps } = props;
    return (React.createElement(Slot.Root, { "data-accent-color": color, ...headingProps, className: classNames('fui-Heading', className, size ? `fui-r-size-${size}` : undefined, weight ? `fui-r-weight-${weight}` : undefined, align ? `fui-r-ta-${align}` : undefined, trim ? `fui-r-lt-${trim}` : undefined, { 'fui-high-contrast': highContrast }) }, asChild ? children : React.createElement(Tag, null, children)));
};
Heading.displayName = 'Heading';
export { Heading };
//# sourceMappingURL=heading.js.map