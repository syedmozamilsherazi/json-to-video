import classNames from 'classnames';
import * as React from 'react';
import { codePropDefs } from './code.props';
const Code = (props) => {
    const { className, size = codePropDefs.size.default, variant = codePropDefs.variant.default, weight = codePropDefs.weight.default, color = codePropDefs.color.default, highContrast = codePropDefs.highContrast.default, ...codeProps } = props;
    return (React.createElement("code", { "data-accent-color": color, ...codeProps, className: classNames('fui-Code', className, `fui-r-size-${size}`, `fui-variant-${variant}`, `fui-r-weight-${weight}`, { 'fui-high-contrast': highContrast }) }));
};
Code.displayName = 'Code';
export { Code };
//# sourceMappingURL=code.js.map