'use client';
import classNames from 'classnames';
import { RadioGroup as RadioGroupPrimitive } from 'radix-ui';
import * as React from 'react';
import { radioGroupPropDefs } from './radio-group.props';
const RadioGroupRoot = (props) => {
    const { className, size = radioGroupPropDefs.size.default, color = radioGroupPropDefs.color.default, highContrast = radioGroupPropDefs.highContrast.default, ...rootProps } = props;
    return (React.createElement(RadioGroupPrimitive.Root, { "data-accent-color": color, ...rootProps, className: classNames('fui-RadioGroupRoot', className, `fui-r-size-${size}`, {
            'fui-high-contrast': highContrast,
        }) }));
};
RadioGroupRoot.displayName = 'RadioGroupRoot';
const RadioGroupItem = (props) => {
    const { children, className, style, ...itemProps } = props;
    const Comp = children ? 'label' : 'span';
    return (React.createElement(Comp, { className: classNames('fui-RadioGroupItem', className), style: style },
        React.createElement(RadioGroupPrimitive.Item, { ...itemProps, className: classNames('fui-reset', 'fui-RadioGroupButton') },
            React.createElement(RadioGroupPrimitive.Indicator, { className: "fui-RadioGroupIndicator" })),
        children));
};
RadioGroupItem.displayName = 'RadioGroupItem';
export { RadioGroupItem as Item, RadioGroupRoot as Root };
//# sourceMappingURL=radio-group.js.map