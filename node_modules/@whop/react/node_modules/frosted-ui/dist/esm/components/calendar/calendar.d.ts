import { CalendarDate, CalendarDateTime, ZonedDateTime } from '@internationalized/date';
import { CalendarStateOptions, RangeCalendarStateOptions } from '@react-stately/calendar';
import React from 'react';
export type MappedDateValue<T> = T extends ZonedDateTime ? ZonedDateTime : T extends CalendarDateTime ? CalendarDateTime : T extends CalendarDate ? CalendarDate : never;
type CalendarProps = Omit<CalendarStateOptions, 'locale' | 'createCalendar'> & {
    className?: string;
};
declare function Calendar({ className, ...props }: CalendarProps): React.JSX.Element;
type RangeCalendarProps = Omit<RangeCalendarStateOptions, 'locale' | 'createCalendar'> & {
    className?: string;
};
declare function RangeCalendar({ className, ...props }: RangeCalendarProps): React.JSX.Element;
export { Calendar, RangeCalendar };
export type { CalendarProps, RangeCalendarProps };
