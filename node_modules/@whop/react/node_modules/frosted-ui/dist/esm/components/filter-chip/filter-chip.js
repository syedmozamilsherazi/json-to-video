'use client';
import classNames from 'classnames';
import { Checkbox as CheckboxPrimitive } from 'radix-ui';
import * as React from 'react';
import { filterChipPropDefs } from './filter-chip.props';
const FilterChip = (props) => {
    const { children, className, style, size = filterChipPropDefs.size.default, color = filterChipPropDefs.color.default, ...checkboxProps } = props;
    return (React.createElement(CheckboxPrimitive.Root, { "data-accent-color": color, ...checkboxProps, className: classNames('fui-reset', 'fui-BaseChip', className, `fui-r-size-${size}`), style: style }, children));
};
FilterChip.displayName = 'FilterChip';
export { FilterChip };
//# sourceMappingURL=filter-chip.js.map