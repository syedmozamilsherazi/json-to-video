import classNames from 'classnames';
import * as React from 'react';
import { badgePropDefs } from './badge.props';
const Badge = (props) => {
    const { className, size = badgePropDefs.size.default, variant = badgePropDefs.variant.default, color = badgePropDefs.color.default, highContrast = badgePropDefs.highContrast.default, ...badgeProps } = props;
    return (React.createElement("span", { "data-accent-color": color, ...badgeProps, className: classNames('fui-Badge', className, `fui-r-size-${size}`, `fui-variant-${variant}`, {
            'fui-high-contrast': highContrast,
        }) }));
};
Badge.displayName = 'Badge';
export { Badge };
//# sourceMappingURL=badge.js.map