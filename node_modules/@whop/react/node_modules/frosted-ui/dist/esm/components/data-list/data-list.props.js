import { colorProp, highContrastProp, trimProp } from '../../helpers';
const alignValues = ['start', 'center', 'end', 'baseline', 'stretch'];
const orientationValues = ['horizontal', 'vertical'];
const sizes = ['1', '2', '3'];
const dataListRootPropDefs = {
    orientation: {
        type: 'enum',
        values: orientationValues,
        default: 'horizontal',
    },
    size: {
        type: 'enum',
        values: sizes,
        default: '2',
    },
    trim: {
        ...trimProp,
    },
};
const dataListItemPropDefs = {
    align: {
        type: 'enum',
        values: alignValues,
        default: undefined,
    },
};
const dataListLabelPropDefs = {
    color: colorProp,
    highContrast: highContrastProp,
};
export { dataListItemPropDefs, dataListLabelPropDefs, dataListRootPropDefs };
//# sourceMappingURL=data-list.props.js.map