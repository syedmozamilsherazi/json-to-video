'use client';
import classNames from 'classnames';
import { Progress as ProgressPrimitive } from 'radix-ui';
import * as React from 'react';
import { progressPropDefs } from './progress.props';
const Progress = (props) => {
    const { className, size = progressPropDefs.size.default, color = progressPropDefs.color.default, highContrast = progressPropDefs.highContrast.default, value = 0, max = 100, ...progressProps } = props;
    const progress = Math.max(0, Math.min((value || 0) / max, 100));
    return (React.createElement(ProgressPrimitive.Root, { "data-accent-color": color, className: classNames('fui-ProgressRoot', className, {
            'fui-high-contrast': highContrast,
        }, `fui-r-size-${size}`), value: value, max: max, ...progressProps },
        React.createElement(ProgressPrimitive.Indicator, { className: "fui-ProgressIndicator", style: { width: `${progress * 100}%` } })));
};
Progress.displayName = 'Progress';
export { Progress };
//# sourceMappingURL=progress.js.map