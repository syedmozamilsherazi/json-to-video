import classNames from 'classnames';
import * as React from 'react';
import { textAreaPropDefs } from './text-area.props';
const TextArea = (props) => {
    const { className, size = textAreaPropDefs.size.default, variant = textAreaPropDefs.variant.default, color = textAreaPropDefs.color.default, style, ...textAreaProps } = props;
    return (React.createElement("div", { "data-accent-color": color, style: style, className: classNames('fui-TextAreaRoot', className, `fui-r-size-${size}`, `fui-variant-${variant}`) },
        React.createElement("textarea", { className: "fui-TextAreaInput", ...textAreaProps }),
        React.createElement("div", { className: "fui-TextAreaChrome" })));
};
TextArea.displayName = 'TextArea';
export { TextArea };
//# sourceMappingURL=text-area.js.map