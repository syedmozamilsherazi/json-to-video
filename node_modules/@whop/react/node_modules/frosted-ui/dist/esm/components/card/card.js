import classNames from 'classnames';
import { Slot } from 'radix-ui';
import * as React from 'react';
import { cardPropDefs } from './card.props';
const Card = (props) => {
    const { asChild, children, className, size = cardPropDefs.size.default, variant = cardPropDefs.variant.default, ...cardProps } = props;
    const Comp = asChild ? Slot.Root : 'div';
    function getChild() {
        const firstChild = React.Children.only(children);
        return React.cloneElement(firstChild, {
            children: React.createElement("div", { className: "fui-CardInner" }, firstChild.props.children),
        });
    }
    return (React.createElement(Comp, { ...cardProps, className: classNames('fui-reset', 'fui-Card', className, `fui-r-size-${size}`, `fui-variant-${variant}`) }, asChild ? getChild() : React.createElement("div", { className: "fui-CardInner" }, children)));
};
Card.displayName = 'Card';
export { Card };
//# sourceMappingURL=card.js.map