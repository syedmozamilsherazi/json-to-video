import classNames from 'classnames';
import * as React from 'react';
import { Text } from '../text';
import { linkPropDefs } from './link.props';
const Link = (props) => {
    const { children, className, asChild = false, underline = linkPropDefs.underline.default, ...linkProps } = props;
    return (React.createElement(Text, { ...linkProps, asChild: true, className: classNames('fui-reset', 'fui-Link', className, `fui-underline-${underline}`) }, asChild ? children : React.createElement("a", null, children)));
};
Link.displayName = 'Link';
export { Link };
//# sourceMappingURL=link.js.map